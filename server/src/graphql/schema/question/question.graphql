extend type Query {
	""" Get a question by id. """
	question(questionId: ID!): Question!
}

""" Question """
type Question {
	id: ID!
	title: String!
	description: String!
	image: Image
}

""" Image Object """
type Image {
	id: ID!
	url: String!
}
